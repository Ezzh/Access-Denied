version: '3'

services:
    mongo:
        image: mongo
        container_name: mongodb
    web:
        depends_on:
            - mongo
        build: ./web/
        volumes:
            - ./web/resources:/usr/src/app/resources
            - ./web/templates:/usr/src/app/templates
            - ./web/secret-data:/usr/src/app/secret-data
        ports:
        - "8000:8000"